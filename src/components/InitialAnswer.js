

function InitialAnswer({handleTransition}) {

    return (
        <div>
            <h2>InitialAnswer</h2>
            <button onClick={handleTransition}>Go next</button>
        </div>
    );
}
export default InitialAnswer;